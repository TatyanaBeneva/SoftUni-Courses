<!-- Dashboard (All Causes View) -->
{{> header}}
{{> notifications}}
<div style="margin-top:3%; padding-top: 3%;" class="container">
    <div style="margin-bottom:25%;" class="row">
        {{#each couses}}
        <div class="col-lg-12">
            <div class="card mt-4">
                <img style="max-width: 1200px; height: auto; border-radius:25px;" class="card-img-top img-fluid"
                    src={{pictureUrl}} alt="">
                <div class="card-body">
                    <h3 class="card-title">{{cause}}</h3>
                    <p class="card-text">{{description}}</p>
                    <a href="#/couse/details/{{id}}" class="btn btn-success">Become a donor {{neededFunds}}</a>
                </div>
            </div>
        </div>
        {{else}}
            <h1 class="text-center">NO CAUSES :(</h1>
        {{/each}}
    </div>
</div>
{{> footer}}
<!-- ./Dashboard (All Causes View) -->