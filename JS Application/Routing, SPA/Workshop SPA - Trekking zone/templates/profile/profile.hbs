{{> header}}

<div class="profile col-md-6 text-center col-lg">
    <img class="profile-img" src="./images/user.png" />
    <div class="profile-info">
        <p>Username: <small>{{username}}</small></p>
        <p class="infoType">Wished {{numberOfTreks}} treks =)</p>
        {{#if isHaveTrek}}
        {{#each treks}}
        <p>{{location}}</p>
        {{/each}}
        {{else}}
        <p>No treks</p>
        {{/if}}
    </div>
</div>

{{> footer}}